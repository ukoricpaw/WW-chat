import { FC } from 'react';
import styles from '../../styles/SingleChat.module.scss';

const ContactInfo: FC = () => {
  return <div className={styles.contactInfo}>ContactInfo</div>;
};

export default ContactInfo;
