import { RootState } from '../store';
import { DataByJoiningToDialog } from '../types/roomTypes';

export default function joinToGroup(state: RootState, data: DataByJoiningToDialog) {
  // state.roomReducer.roomType = 'group';
  // state.roomReducer.groupInfo = data.data.
}
